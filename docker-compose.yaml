services:
  db:
    build: ./
    restart: always
    env_file: ./server/src/main/resources/.env
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      - my-db:/var/lib/mysql
volumes:
  my-db: