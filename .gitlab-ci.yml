stages:
  - build
  - test
  - deploy

variables:
  MAVEN_CLI_OPTS: "--batch-mode"
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository

build-job:
  stage: build
  image: maven:3.8.6-openjdk-18
  before_script:
    - echo "Setting up Docker..."
    - docker info
    - echo "Changing ownership of files..."
    - sudo chown -R $(whoami) .
  script:
    - echo "Starting Maven build for all modules..."
    - mvn $MAVEN_CLI_OPTS clean install -DskipTests
  artifacts:
    paths:
      - target/
    expire_in: 2 days

unit-test-job:
  stage: test
  image: maven:3.8.6-openjdk-18
  before_script:
    - echo "Setting up Docker..."
    - docker info
    - echo "Changing ownership of files..."
    - sudo chown -R $(whoami) .
  script:
    - echo "Running unit tests across all modules..."
    - mvn $MAVEN_CLI_OPTS verify
  artifacts:
    when: always
    paths:
      - target/
    expire_in: 2 days
    reports:
      junit: "**/target/surefire-reports/*.xml"
  coverage: '/Total\s+coverage:\s+(\d+\.\d+)%/'

deploy-job:
  stage: deploy
  image: docker:latest
  services:
    - docker:dind
  before_script:
    - echo "Setting up Docker..."
    - docker info
    - echo "Changing ownership of files..."
    - sudo chown -R $(whoami) .
  script:
    - echo "Deploying the application..."
    - docker login
    - docker build -t timpodszus/iberia-swp server/.
    - docker push timpodszus/iberia-swp
    - docker build -t timpodszus/iberia-db .
    - docker push timpodszus/iberia-db